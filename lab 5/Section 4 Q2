#include <stdio.h>

int main() {
    int a;
    int count;
    printf("Enter a: ");
    scanf("%d", &a);
    while (a != 0) {
        if (a & 1) {
            count += 1;
        }
        a = a >> 1;
    }
    printf("number of 1s: %d", count);
    return 0;
}
