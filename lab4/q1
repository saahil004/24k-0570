#include <stdio.h>

int main() {
    int num;
    printf("enter num: \n");
    scanf("%d", &num);
    if (num % 3 == 0) {
        printf("number is  multiple of 3\n");
    } else {
        printf("not a multiple of 3\n");
    }
    return 0;
}
