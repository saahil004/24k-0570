#include <stdio.h>

int sum(int a, int b);

int sum(int a, int b) {
	return a+b;
}

int main() {
	int a, b;
	printf("Enter a: ");
	scanf("%d", &a);
	printf("Enter b: ");
	scanf("%d", &b);
	int s = sum(a, b);
	printf("The sum is: %d", s);
	return 0;
}
